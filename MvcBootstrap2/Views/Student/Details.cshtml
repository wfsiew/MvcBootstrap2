@model MvcBootstrap2.Models.Student

@using MvcBootstrap2.Models;

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div class="well">
    <form class="form-horizontal" role="form">
        <fieldset>
            <legend>Student</legend>

            <div class="control-group">
                <label class="control-label">@Html.DisplayNameFor(model => model.LastName)</label>
                <div class="controls">
                    @Html.DisplayFor(model => model.LastName)
                </div>
            </div>
            <div class="control-group">
                <label class="control-label">@Html.DisplayNameFor(model => model.FirstMidName)</label>
                <div class="controls">
                    @Html.DisplayFor(model => model.FirstMidName)
                </div>
            </div>
            <div class="control-group">
                <label class="control-label">@Html.DisplayNameFor(model => model.EnrollmentDate)</label>
                <div class="controls">
                    @Html.DisplayFor(model => model.EnrollmentDate)
                </div>
            </div>
            @*<div class="control-group">
                <label class="control-label">@Html.DisplayNameFor(model => model.Enrollments)</label>
                <div class="controls">
                    <table class="table table-striped table-hover table-condensed">
                        <thead>
                            <tr>
                                <th>Course Title</th>
                                <th>Grade</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (Enrollment item in Model.Enrollments)
                            {
                                <tr>
                                    <td>@Html.DisplayFor(x => item.Course.Title)</td>
                                    <td>@Html.DisplayFor(x => item.Grade)</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>*@
        </fieldset>
    </form>
</div>

<div class="navbar">
    <div class="navbar-inner">
        <ul class="nav">
            <li><a href="@Url.Action("Edit", new { id = Model.Id })"><i class="icon-pencil icon-white"></i> Edit</a></li>
            <li><a href="@Url.Action("Index")"><i class="icon-arrow-left icon-white"></i> Back to List</a></li>
        </ul>
    </div>
</div>
